- 여기까지 됨: `vsx.js` 의 status / pack / install / docs. `docs` 는 dev 폴더 아래 git 저장소를 전부 돌며 CLAUDE.md 가 있는지, 그 지도가 가리키는 경로가 아직 있는지, 지도가 바뀐 뒤 커밋이 얼마나 쌓였는지 봄. 경로 판정은 `node vsx.js docs --selftest` 가 지킴. 설치 페이지 `index.html` — 확장을 세 갈래(네이티브 코드 / 코드 읽기 / 에디터 주변)로 묶고 카드 격자 배치
- 다음 할 것: 실제 확장 하나로 `install` 을 돌려 설치까지 확인. Pages 주소에서 설치 버튼이 VS Code 를 여는지 확인. `docs` 오탐(릴리스 주소, 빌드 임시 폴더)이 거슬리면 그때 규칙을 더 조이기
- 막힌 것: 없음. `docs` 는 저장소 12개에서 지적 10건을 내고 그중 3건이 진짜였음. 보고서로 쓰기엔 충분하지만 막는 검사로 쓰기엔 모자라서 종료 코드는 늘 0
